# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: MSFT Lab

steps:
- script: |
     hostname
     pushd /home/dw0647/stratumaosm/stratum/aosm
     #./deploy_operator.sh
  displayName: 'deploy'
